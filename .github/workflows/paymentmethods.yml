name: CI - Payment Methods Service

on:
  push:
    branches: [ main ]
    paths:
      - 'API.PaymentMethods/**'
      - 'API.Core/**'
      - '.github/workflows/paymentmethods.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'API.PaymentMethods/**'
      - 'API.Core/**'
      - '.github/workflows/paymentmethods.yml'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Restore dependencies
      run: dotnet restore API.PaymentMethods/API.PaymentMethods.csproj

    - name: Build
      run: dotnet build API.PaymentMethods/API.PaymentMethods.csproj -c Release --no-restore

    - name: Run Tests
      run: dotnet test API.Tests/API.Tests.csproj --filter "FullyQualifiedName~API.Tests.Features.PaymentMethod"

    - name: Build Docker Image Check
      run: docker build -f API.PaymentMethods/Dockerfile -t api-paymentmethods:${{ github.sha }} .

    # ==========================================
    # FUTURE RELEASE / AWS DEPLOYMENT TEMPLATE
    # ==========================================
    # - name: Configure AWS Credentials
    #   uses: aws-actions/configure-aws-credentials@v4
    #   with:
    #     role-to-assume: arn:aws:iam::123456789012:role/github-actions-ecr-role
    #     aws-region: us-east-1
    #
    # - name: Login to Amazon ECR
    #   id: login-ecr
    #   uses: aws-actions/amazon-ecr-login@v2
    #
    # - name: Push to ECR
    #   run: |
    #     docker tag api-paymentmethods:${{ github.sha }} ${{ steps.login-ecr.outputs.registry }}/api-paymentmethods:${{ github.sha }}
    #     docker tag api-paymentmethods:${{ github.sha }} ${{ steps.login-ecr.outputs.registry }}/api-paymentmethods:latest
    #     docker push ${{ steps.login-ecr.outputs.registry }}/api-paymentmethods --all-tags
